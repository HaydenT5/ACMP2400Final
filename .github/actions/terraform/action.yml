name: 'Terraformer'
description: 'Executes terraform plan and apply steps'
author: 'HaydenT5@'
inputs:
  ARM_CLIENT_ID:
    description: 'The Azure Client ID'
    required: true
  ARM_CLIENT_SECRET:
    description: 'The azure cleint secret'
    required: true
  ARM_SUBSCRIPTION_ID:
    description: 'the azure sub'
    required: true
  ARM_TENANT_ID: 
    description: 'the azure tenant id'
    required: true
  STATE_KEY: 
    description: 'the unique key for state lookup'
    required: true
  TF_STAGE:
    description: 'the TF deployment stage'
    required: true
  DJANGO_SECRET_KEY_PROD:
    description:: 'the secret key for django to startup'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
